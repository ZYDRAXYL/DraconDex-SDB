node_modules/
*.log

# Databases. Nothing in this repo should ever hold real user content — the
# schema is the contract, never the data.
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Secrets — none are committed and none should be.
.env
.env.*
*.pem
*.key
