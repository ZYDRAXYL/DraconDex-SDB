{
  "name": "@zydraxyl/dracondex-sdb",
  "version": "1.0.0",
  "private": true,
  "description": "DraconDex central SQLite schema — the one source both the Electron and Flutter apps generate their vault DDL from",
  "license": "MIT",
  "type": "module",
  "repository": { "type": "git", "url": "https://github.com/ZYDRAXYL/DraconDex-SDB.git" },
  "scripts": {
    "generate": "node schema/generate.mjs && node supabase/setup/gen.mjs && node tools/build-manifest.mjs",
    "check": "node schema/generate.mjs --check && node supabase/setup/gen.mjs --check && node tools/build-manifest.mjs --check",
    "chain:survey": "node tools/chain-survey.mjs",
    "chain:propagate": "node tools/chain-propagate.mjs"
  }
}
